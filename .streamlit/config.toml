[server]
# Max size, in megabytes, for files uploaded with the file_uploader.
# Default: 200
maxUploadSize = 200

# Max size, in megabytes, of messages that can be sent via the WebSocket connection.
# Default: 200
maxMessageSize = 200

[theme]
primaryColor="7b83eb"
backgroundColor="#ffffff"
secondaryBackgroundColor="5059c9"
textColor="#000000"
font="sans serif"
